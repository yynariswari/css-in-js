import styled, { css } from "styled-components";

export const colors = {
  red: "#ff3b30",
  blue: "#0d6efd",
};

export const ButtonBase = css`
  padding: 10px 18px;
  border-radius: 8px;
  font-size: 16px;
  cursor: pointer;
  background: #fff;
  border: 2px solid;
  transition: 0.2s;
`;
